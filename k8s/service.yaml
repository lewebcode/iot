apiVersion: v1
kind: Service
metadata:
  name: go-microservice
  labels:
    app: go-microservice
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: go-microservice

